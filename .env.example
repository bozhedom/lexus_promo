# Скопируйте в .env и заполните. Сам .env в репозиторий не попадает.

# ─── Обязательное ──────────────────────────────────────────────────────────

# База данных. Локально поднимается командой: docker compose up -d
DATABASE_URL=postgres://promo:promo@localhost:5432/promo

# Секрет Payload. Сгенерировать: openssl rand -hex 32
PAYLOAD_SECRET=change-me

# Пользователь админки. `npm run admin:reset` заводит его или меняет пароль на
# указанный здесь — так пароль всегда под рукой и не хранится в репозитории.
PAYLOAD_ADMIN_EMAIL=admin@example.com
PAYLOAD_ADMIN_PASSWORD=change-me

# ─── Определение авто по госномеру ─────────────────────────────────────────

# Внешний API, ходим через прокси /api/car-info. Если пусто — определение
# выключено и воронка сразу просит заполнить марку и модель вручную.
CAR_API_URL=http://dev.api2.club.mg/cars
CAR_API_KEY=

# ─── Подтверждение телефона по СМС ────────────────────────────────────────

# Ключ API сервиса SMS.ru. Если пусто, в dev/test код показывается в окне;
# production без ключа не отправляет и не раскрывает код.
SMS_RU_API_ID=
# Необязательное предварительно согласованное имя отправителя.
SMS_RU_FROM=
# Отдельный секрет подписи одноразовых кодов. Сгенерировать: openssl rand -hex 32
# Если пусто, используется PAYLOAD_SECRET.
PHONE_VERIFICATION_SECRET=

# ─── Аналитика ─────────────────────────────────────────────────────────────

# Яндекс.Метрика.
NEXT_PUBLIC_YM_ID=
# Google Analytics (опционально)
NEXT_PUBLIC_GA_ID=

# ─── Внешние ссылки итогового экрана ───────────────────────────────────────
# Если пусто, кнопка ведёт на «#»

NEXT_PUBLIC_LINK_CALC=
NEXT_PUBLIC_LINK_BOOKING=
NEXT_PUBLIC_LINK_BOOK=
NEXT_PUBLIC_LINK_MESSENGER=
NEXT_PUBLIC_LINK_MAP=
NEXT_PUBLIC_LINK_SITE=

# ─── Прочее ────────────────────────────────────────────────────────────────

# Публичный адрес сайта, подставляется в метаданные страниц.
# Он же используется как база для адресов вебхуков модуля /invite-test,
# поэтому на боевом сервере это должен быть настоящий https-домен.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Стартовое раскрытие штор руками. false — первый экран появляется сразу.
NEXT_PUBLIC_INTRO_CURTAIN_ENABLED=false

# ─── Мессенджеры (модуль invite-test) ──────────────────────────────────────
# Полная инструкция: src/invite-test/README.md.
#
# Основной путь — инстансы GREEN-API. У сервиса отдельный продукт под каждый
# мессенджер, но реквизиты и методы одинаковые, поэтому от канала нужны одни и
# те же три поля из консоли console.green-api.com. Инстанс привязан к личному
# аккаунту менеджера: пригласительные приходят гостю из диалога с человеком.

INVITE_TEST_WA_GREEN_INSTANCE_ID=
INVITE_TEST_WA_GREEN_API_TOKEN=
# Уникальный apiUrl инстанса, например https://7105.api.greenapi.com
INVITE_TEST_WA_GREEN_API_URL=

INVITE_TEST_TG_GREEN_INSTANCE_ID=
INVITE_TEST_TG_GREEN_API_TOKEN=
INVITE_TEST_TG_GREEN_API_URL=

INVITE_TEST_MAX_GREEN_INSTANCE_ID=
INVITE_TEST_MAX_GREEN_API_TOKEN=
INVITE_TEST_MAX_GREEN_API_URL=

# Общий на все инстансы; /setup сам передаст его GREEN-API для защиты вебхука.
INVITE_TEST_GREEN_WEBHOOK_TOKEN=

# Куда ведёт кнопка мессенджера. Ссылка собирается по аккаунту самого
# инстанса (getAccountSettings): писать гость должен тому, кто слушает вебхук.
# Заполнять нужно только MAX — username оттуда приходит не всегда, а
# телефонного диплинка у мессенджера нет.
INVITE_TEST_MAX_MANAGER=
# Запасные значения, если аккаунт инстанса почему-то не отдался.
INVITE_TEST_TG_MANAGER=
INVITE_TEST_WA_PHONE=

# Ключ для разового вызова /api/invite-test/setup (прописывает вебхуки)
INVITE_TEST_SETUP_KEY=

# ─── Запасной путь: боты ───────────────────────────────────────────────────
# Нужны, только если инстанса GREEN-API для канала нет: тогда отвечает бот, а
# не менеджер. Как только инстанс заведён, бот не используется.

# Токен бота от @BotFather (/newbot). Username бота спрашиваем у Telegram сами.
INVITE_TEST_TG_BOT_TOKEN=
# Произвольная строка, Telegram присылает её в заголовке вебхука
INVITE_TEST_TG_WEBHOOK_SECRET=
# Необязательно. Идентификатор бизнес-подключения: с ним бот отвечает от имени
# менеджера. Приходит сам, когда менеджер подключает бота в Telegram Business.
INVITE_TEST_TG_BUSINESS_ID=
# Необязательно. Перебивает username бота, если ответ Telegram не устроил.
INVITE_TEST_TG_BOT_USERNAME=

# Токен бота MAX из кабинета business.max.ru. Username берётся по нему.
INVITE_TEST_MAX_BOT_TOKEN=
# Секрет вебхука: от 5 символов, только латиница, цифры, «_» и «-».
INVITE_TEST_MAX_WEBHOOK_SECRET=
INVITE_TEST_MAX_API_URL=
# Необязательно. Перебивает username бота, если ответ MAX не устроил.
INVITE_TEST_MAX_BOT_USERNAME=
